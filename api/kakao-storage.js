import{isAuthorized}from"../lib/auth.js";import{listBuckets}from"../lib/kakao-storage.js";export default async function handler(request,response){if(!await isAuthorized(new Request("https://work-memory/api/kakao-storage",{headers:{cookie:request.headers.cookie||""}})))return response.status(401).json({error:"unauthorized"});if(request.method!=="GET")return response.status(405).json({error:"method_not_allowed"});try{return response.status(200).json({ok:true,items:await listBuckets()});}catch(error){return response.status(502).json({error:error.message});}}
